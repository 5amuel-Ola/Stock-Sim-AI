[build]
builder = "NIXPACKS"
buildCommand = "cd financial-ai-backend && npm install && npx prisma generate && npm run build"

[deploy]
startCommand = "cd financial-ai-backend && npm start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
